# Notification Center

A popup notification inbox. Displays a list of notifications with app name, summary, and body text. Clicking a notification dismisses it. A Clear All button removes all notifications. Uses mock data via a ListModel.
