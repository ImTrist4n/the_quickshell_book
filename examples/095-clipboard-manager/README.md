# Clipboard Manager

A clipboard history popup. Stores recent clipboard entries in a list and displays them. Click an entry to restore it to the clipboard. Uses a debounce timer to avoid re-capturing its own clipboard writes. Persistence is mocked with a simple ListModel.
