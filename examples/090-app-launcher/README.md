# App Launcher

A keyboard-driven application launcher popup. Displays a filtered list of applications as you type. Navigate with Up/Down, launch with Enter, close with Escape.

Uses a hardcoded list of mock applications for standalone demonstration.
