# Lock Screen

A full-screen lock screen overlay. Displays the current time and date in large typography, a password input field, and a placeholder area for media player info (Now Playing). The password field uses masked input for privacy.
