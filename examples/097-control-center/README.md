# Control Center

A quick-settings popup with toggle cards for Wi-Fi, Bluetooth, Dark Mode, and Do Not Disturb. Each `ToggleCard` is a reusable component with an icon, label, and animated color transitions between on/off states.
